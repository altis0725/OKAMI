# OKAMI Production Environment Configuration Example
# Copy this file to .env.production and configure with your actual values

# === Monica API Configuration (Required) ===
# Monica LLM API key for GPT-4o compatible interface
MONICA_API_KEY=your_monica_api_key_here
MONICA_BASE_URL=https://monica.im/v1

# === OpenAI Configuration (Required for embeddings) ===
# OpenAI API key for text embeddings
OPENAI_API_KEY=your_openai_api_key_here

# === ChromaDB Configuration ===
# Vector database settings
CHROMA_HOST=chromadb
CHROMA_PORT=8000
CHROMA_AUTH_TOKEN=generate_secure_token_here
CHROMA_AUTH_PROVIDER=chromadb.auth.token.TokenAuthServerProvider
CHROMA_PERSIST_DIRECTORY=/app/storage/chroma

# === Server Configuration ===
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
LOG_LEVEL=INFO
PRODUCTION=true
ALLOWED_HOSTS=traning.work,www.traning.work,138.2.45.112,localhost

# === CrewAI Configuration ===
CREWAI_STORAGE_DIR=/app/storage
CREWAI_LOG_LEVEL=INFO
CREWAI_DISABLE_TELEMETRY=true
OTEL_SDK_DISABLED=true

# === Embeddings Configuration ===
# Use OpenAI for production, Ollama for local development
EMBEDDER_PROVIDER=openai
EMBEDDER_MODEL=text-embedding-3-small

# === Storage Paths ===
STORAGE_BASE_PATH=/app/storage
KNOWLEDGE_BASE_PATH=/app/knowledge
EVOLUTION_BASE_PATH=/app/evolution

# === Memory Configuration ===
MEMORY_PROVIDER=basic
MEMORY_DB_PATH=/app/storage/memory.db

# === Vector Store Configuration ===
VECTOR_STORE_TYPE=chroma
USE_QDRANT=false

# === Security ===
# Generate unique values for production
SECRET_KEY=generate_random_hex_32_here
JWT_SECRET=generate_random_hex_32_here

# === Performance ===
MAX_WORKERS=4
REQUEST_TIMEOUT=300
TASK_TIMEOUT=600

# === Monitoring ===
ENABLE_METRICS=true
METRICS_PORT=9090

# === Optional: Ollama Configuration ===
# Uncomment if using Ollama for local embeddings
# OLLAMA_HOST=http://ollama:11434
# OLLAMA_BASE_URL=http://ollama:11434
# OLLAMA_MODEL=llama2

# === Optional: MCP Gateway Configuration ===
# Uncomment if using MCP tools
# MCP_GATEWAY_URL=http://mcp-gateway:5173
# MCP_ENABLED=true

# === Optional: Quality Check Interval ===
QUALITY_CHECK_INTERVAL=30

# === Optional: Development Tools ===
# Set to true only for debugging
DEBUG=false
RELOAD=false